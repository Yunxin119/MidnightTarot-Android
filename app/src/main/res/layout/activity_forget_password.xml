<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:background="@drawable/gradient_main">

    <LinearLayout
        android:id="@+id/ll_forget_password_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp"
        android:gravity="center">

        <!-- App logo -->
        <ImageView
            android:id="@+id/iv_forget_password_logo"
            android:layout_width="180dp"
            android:layout_height="180dp"
            android:layout_marginBottom="32dp"
            android:contentDescription="App Logo"
            android:src="@drawable/launch" />

        <!-- Header text -->
        <TextView
            android:id="@+id/tv_forget_password_header"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Reset Password"
            android:textSize="28sp"
            android:textStyle="bold"
            android:textColor="@color/text_primary"
            android:gravity="center"
            android:layout_marginBottom="16dp"/>

        <!-- Email input -->
        <EditText
            android:id="@+id/et_forget_password_email"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/textarea"
            android:hint="Enter your email"
            android:inputType="textEmailAddress"
            android:textColor="@color/text_primary"
            android:textColorHint="@color/text_accent"
            android:layout_marginBottom="32dp"
            android:padding="16dp"/>

        <!-- Reset button -->
        <Button
            android:id="@+id/btn_forget_password_reset"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Send Reset Link"
            android:textSize="16sp"
            android:textAllCaps="false"
            android:padding="16dp"
            android:layout_marginBottom="16dp"
            app:cornerRadius="8dp"
            android:background="@drawable/button_glow_effect"
            android:foreground="@drawable/button_stroke"
            app:backgroundTint="@color/primary"/>

        <!-- Back button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_forget_password_back"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Back"
            android:textColor="@color/text_secondary"
            android:textSize="16sp"
            android:textAllCaps="false"
            android:padding="8dp"
            app:cornerRadius="8dp"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:background="@android:color/transparent"/>

        <!-- Instruction text -->
        <TextView
            android:id="@+id/tv_forget_password_instruction"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="We'll send you an email to reset your password"
            android:textColor="@color/text_secondary"
            android:textSize="14sp"
            android:gravity="center"
            android:layout_marginTop="24dp"
            android:alpha="0.8"/>

    </LinearLayout>
</ScrollView>